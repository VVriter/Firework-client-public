package com.firework.client.Implementations.UI.Particles;

public class ParticleInfo {
    //Is enabled
    public static boolean isEnabled = false;

    //Draw mode
    public static colorMode mode = colorMode.Default;
    public enum colorMode{
        Default, Astolfo
    }
}
